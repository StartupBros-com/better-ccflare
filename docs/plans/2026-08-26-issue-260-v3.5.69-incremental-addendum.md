# Issue 260: upstream v3.5.69 incremental merge addendum

## Immutable merge facts

| Fact | Value |
| --- | --- |
| Upstream tag | `upstream-v3.5.69` |
| Upstream target | `4d27cb226f383a39e12aea530e83d2f9896999ce` |
| Merge base | `31da6f0b72400df47b71aa170530390b46aafbda` |
| Current first parent | `67168aa4bdee005f5748842c0b25adca0a9494a9` |
| Upstream delta | 12 commits, 43 paths |
| Initial conflict inventory | 21 paths |

The target is `upstream-v3.5.69`, whose terminal commit is `4d27cb22` (`chore: bump version for deployment`). The merge is intentionally a genuine two-parent merge: the eventual merge commit must retain `67168aa4…` as first parent and `4d27cb226…` as second parent. Do not replace that topology with a squash, rebase replay, or fast-forward.

## Semantic dispositions

1. **Adopt upstream DeepSeek and OpenAI-compatible model-discovery work.** Preserve the upstream provider registration, account mode, default model, and package release lineage; package versions in both root and CLI manifests are `3.5.69`.
2. **Retain fork contracts where upstream documentation predates them.** The root README and provider/CLI documentation retain canonical Meta wording (`meta`; legacy `muse-spark` is HTTP-ingress compatibility only), Managed Routing and route profiles, Anthropic degraded-mode guidance, SQLite/PostgreSQL persistence, distribution/update guidance, and current xAI, Codex, and Qwen behavior. DeepSeek documentation is additive.
3. **Keep mode documentation authoritative.** CLI mode lists match `ACCOUNT_MODES`: `claude-oauth`, `console`, `zai`, `minimax`, `deepseek`, `anthropic-compatible`, `openai-compatible`, `nanogpt`, `xai`, `ollama`, and `meta`.
4. **Retain first-parent Claude Code user-agent compatibility.** `packages/core/src/version.ts` keeps `CLAUDE_CLI_VERSION = "2.1.245"`; no unrelated target change in that file is discarded.
5. **Resolve each conflict by behavior, not wholesale side selection.** The 21-path inventory covers account add/remove, server lifecycle, dashboard and CLI surfaces, format conversion, provider registration/model defaults, proxy catalog/token/stream handling, and the three owned documentation files.

## Prohibited path

`apps/cli/README.md` is mechanically resolved by the host and is a no-read/no-touch path for this merge packet. It must not be inspected, quoted, edited, staged, or otherwise included in this resolution work.

## Test plan

1. Confirm the owned files contain no conflict markers and that both manifests report `3.5.69`.
2. Run `bun run lint`, `bun run typecheck`, and `bun run format` after all merge packets are resolved; do not let formatting rewrite unrelated files without review.
3. Run the DeepSeek/provider and affected proxy/CLI suites selected by the final conflict resolutions. Do not send scripted traffic to Anthropic-backed accounts; use no provider traffic for this documentation/version packet.
4. Verify the completed merge commit has exactly the documented first and second parents before any later commit or push.

## Boundaries

This addendum neither deploys nor closes Issue 260. Deployment remains a separate, explicit production action after a merged main commit; the issue remains open until the reporter confirms the fix.
