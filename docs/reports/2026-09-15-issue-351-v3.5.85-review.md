# Final source review receipt: upstream v3.5.85

This records scope-limited independent review of the integration against fork parent d61ce67bf and upstream target80b593e8. No unresolved actionable findings remained in the reviewed scopes at closure. The integration owner separately owns the full test sweep, source inventory and CI.

## Reviewed scopes

- API/server authorization, account setup identity and polling generations; dashboard threshold/transformer controls and preserved pricing/Window Value behavior.
- SQLite rebuild migrations and credential-paired timestamp deduplication in both SQLite/PostgreSQL implementations.
- Default-off transient 5xx recovery and response-model aliasing; physical reservations, hosted dispatch, replay/drain ownership, durable cooldown and receipt truthfulness.
- Responses forced tool-choice admission and native Codex declaration/choice/history identities, trusted metadata boundaries, continuation ownership and digest order.
- Upstream usage polling/reset changes and fork generation/fencing contracts; documentation and release delta.

## Findings corrected and independently rechecked

1. Legacy SQLite table rebuilds dropped new and existing account fields. Both projections now preserve them; manual reauthentication timestamps follow the exact credential donor, including null, on both backends.
2. Malformed or mismatched forced tool choices could produce 500 or forward an undeclared bridge identity. Exact kind/name/namespace admission now returns400 before provider dispatch.
3. Native Codex declarations, forced choice and replay history had inconsistent compatibility aliases. Verified private metadata now restores native identities after ordinary validation/conversion, before continuation ownership/digests; additional tools no longer emit duplicate bridges.
4. Transient 5xx cooldown returned before durable persistence. Bounded awaited persistence now precedes return/failover, and circuit accounting honors actual application.
5. Durable forward-guard suppression and coalesced writes could give a caller another caller's success receipt. Direct suppressed writes and active/tied/superseded followers now return applied=false; only the owning effective writer may claim the mutation.

Independent closure suites passed80/80: migration rebuild6, tool-choice25, native tool identity15, cooldown persistence8, transient5xx26. Original offline reproductions were rerun and passed. Broader worker/root receipts are in the [test manifest](../plans/2026-09-15-issue-351-v3.5.85-test-manifest.json).

## Limits

Independent checks used local synthetic callbacks and in-memory SQLite; the reviewer source-reviewed PostgreSQL corrections and relied on separate worker/root live-PG receipts. The expanded PostgreSQL run has one known failing stale-predecessor test reproduced unchanged on the original fork; this is explicitly retained in the test manifest, not represented as a green PG sweep.

Production service health, real credentials/OAuth exchanges, natural traffic, runtime activation and live providers were not exercised. This receipt does not claim exhaustive manual inspection of every repository file.


# Later main composition closure

# Independent closure: composed main advances #352 and #353

Reviewed source: /home/will/SITES/better-ccflare/.claude/worktrees/upstream-v3.5.85 at HEAD 33cb6ed874aad1b47623902daa6f91147f325ebb with MERGE_HEAD cb651ede4840884f3b7f784fa1291a7ab296c8b9 (subsequent clean staged merge of current main). No repository edits. Read both main-advance patches and the exact composed production source and focused test assertions.

## Result

No unresolved actionable findings in this bounded composition review. The later main changes safely compose with the previously reviewed upstream integration at this source state.

## Selected-provider observation (#352)

The authenticated makeAttemptRequest helper now receives the immutable, non-null selected account provider already used by planning/body conversion. It no longer invokes the ingress ctx.provider observer when a selected Codex account handles an Anthropic-shaped request. The only helper call lives in executeCacheAwareProviderAttempt, covering initial physical HTTP sends and repaired compatibility retries with the corresponding final wire request and current replay body. Successful optional WebSocket sends and synthetic responses still bypass HTTP observation. Unauthenticated forwarding correctly retains ctx.provider.

Inspected the overlap with scalar replay snapshots and retry rebuilds: observation receives the fresh physical Request after conversion and sanitization; it does not reuse consumed retry bodies or alter continuation/native-header authority. Physical reservation, hosted-dispatch ownership, response accounting and transient-5xx behavior are unchanged by this main delta. Tests assert a mismatched ingress provider observes Codex HTTP fallback exactly once, skips the successful WebSocket send, and observes both final bodies/statuses on the compatibility retry.

## Observer response ownership (#353)

makeProxyRequest creates a dedicated responseDrainController for each fetch and registers it against that response. observeCodexWire takes the original response body's sole reader rather than cloning a parallel response branch. Transferring that exact controller to the observer-created wrapper therefore follows the existing same-body/sole-owner contract. Non-success and bodyless responses return the original response and keep their original registration. The composed proxy subsequently transfers ownership to its tagging wrapper and supplies it to provider response processing or bounded discarded-body drains.

The transfer restores cancellation authority previously lost when #352 activates the selected Codex observer. It does not create a new global abort source, grant concurrent clone branches authority, or use one retry's controller for another retry. The new stalled-transport regression proves a drain deadline aborts the registered source transport, settles the pending read, releases the outer reader lock, and records the resulting transport error. Cache-wire source and test match cb651ede exactly. Diagnostics opt-in behavior is unchanged.

## Independent verification

- packages/proxy/src/handlers/__tests__/proxy-operations-codex-websocket.test.ts: 32 passed, 0 failed, 417 assertions. Log: /tmp/ccflare-review-main-advance-websocket.log.
- packages/providers/src/providers/codex/cache-wire.test.ts: 7 passed, 0 failed, 56 assertions. Log: /tmp/ccflare-review-main-advance-cache-wire.log.
- Scoped staged diff whitespace check passed for the four changed source/test files.

All tests used mocked fetch/WebSocket paths or synthetic in-memory streams. Production deployment/service health, actual provider requests, real OAuth exchanges and natural traffic were NOT exercised. Broad static gates, combined test sweeps, inventory completeness and final merge acceptance remain parent-owned.

## Numeric CLI fallback followup

Current source and parity assertion now both use upstream 2.1.272. Prior review found no concrete reason to retain 2.1.250: repeated first-parent upstream syncs advanced the numeric fallback, its test explicitly describes upstream parity, and reviewed consumers use the constant as a User-Agent or latest-client-version fallback rather than a numeric feature gate. This change is distinct from root/CLI package release lineage and preserves the fork's provenance functions. Earlier detailed reasoning remains at /tmp/ccflare-v3.5.85-main-advance-review.md. No live provider/client behavior was tested for this fallback change.
