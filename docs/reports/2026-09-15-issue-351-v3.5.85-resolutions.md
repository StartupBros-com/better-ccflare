# Domain resolution evidence

These are checkpoint receipts; the final integration report and test manifest supersede intermediate counts or pending notes. All verification was offline or against disposable local databases.


# Resolution receipt: api-ui-resolution

# API, server, dashboard and documentation integration

Resolved and staged owned paths only; no commit, push, release, provider traffic or deployment performed.

## Semantic resolutions

- `apps/server/src/server.ts`: retained fork dashboard-auth classification, body admission, shutdown draining, routing-transition recorder, retention cleanup, poll registration leases, deletion/recreation generation checks, endpoint eligibility teardown, canonical snapshots, alerts and Window Value ledger. Upstream pause thresholds run from payload.data in the existing generation-fenced snapshot callback. Pausing/resuming passes expectedCreatedAt through newly fenced DB operations. Unsupported provider/custom Codex endpoints are rejected.
- Upstream `createRefreshBackedTokenProvider` and `createUsageSnapshotRecorder` wrappers were not wired as parallel implementations: fork `refreshPollingAccessToken` already reloads token state without toggling pause, rejects deleted/recreated rows, and fork snapshot dispatcher retains the stronger canonical/generation contracts. Their duplicated upstream standalone test blocks were replaced by existing fork coverage and new threshold tests, while retaining upstream account eligibility and threshold decision tests.
- `codex-usage-refresher.ts`: upstream free-first helper is now the production manual refresh path. Retained free-endpoint 429 stop (never paid fallback), configured rollover session reset and n/a missing percentages; composed account generation/provider/endpoint checks before/after fetch, fork endpoint normalization, failed free-fetch fallback, actual cooldown indication, forward-only atomic response reset persistence including rate_limit_reset_at + optional generation, and generation-fenced canonical history/alerts/Window Value ledger. Introspection preserves elapsed reset evidence unless this refresher handled a real rollover; successful polled snapshots do not write rate_limit_reset.
- HTTP: kept fork immutable AccountCreated response identity and durable device-setup coordinator, terminal-auth authority (`pause_reason`), admin/API authorization and redaction paths. Adapted new-account polling kickoff into server-device-setup commit composition instead of resurrecting upstream ephemeral device sessions. Polling kickoff is implemented in services/usage-polling-start.ts; upstream handlers path is a compatibility reexport to preserve the service-to-handler dependency rule.
- Public account listing and threshold handler expose/use actual poller eligibility (`usagePauseSupported`), including rejection for custom Codex endpoints. UI uses that server capability and preserves provider fallback for older servers.
- Dashboard: composed threshold and max_tokens transformer controls with existing managed-routing invalidation/mutations, native/family quota displays, terminal reauth primary action, Window Value and pricing unknown-vs-zero behavior. Error propagation keeps both dialogs open on failed writes. Restored real Codex five-hour progress only when payload reports numeric utilization and a reset; weekly-only snapshots remain free of synthetic 0% rows.
- Reauth: added deadline sweep/alerts and account badges while retaining fork alert unique-insert/SSE/webhook winner dedup and generation-aware Window Value alert state. Updated fixture methods to real runWithChanges seam and timer tests for independent anomaly + reauth timers.
- Clean two-sided audits: health uses paired representative usage snapshots; AccountList adds callbacks without replacing routing props; RateLimitProgress preserves code-review/live countdown and throttle surfaces; new API routes remain behind shared dashboard authorization. RequestCostBadge/TokenUsageDisplay source paths remain unchanged.

## Documentation/workflow resolutions

- README keeps fork continuation authority and hosted capability restrictions, adds custom freeform tool bridge. Existing contributor credit preserved and upstream #460/#461/#462/#464/#465/#466/#468 credits appended.
- docs/cli keeps fork routing/device flows and adds pause threshold usage. docs/configuration retains all fork controls, new poll interval + actual-window notes; documents default-off CCFLARE_SERVER_ERROR_RETRY_ENABLED and CCFLARE_RESPONSE_MODEL_ALIAS_ENABLED. docs/models alias claim gated accordingly.
- providers docs retain Meta/custom fork identity and upstream Codex free polling information, corrected to fork generation-fenced canonical snapshot dispatch; 429 manual introspection explicitly avoids paid fallback. Routing docs preserve capabilities/degraded mode and gate 5xx handling behind fork authorized attempt/replay contracts.
- codex-vscode doc retained client setup/custom-tool guidance but replaced inapplicable upstream raw/native unknown-field validation claims with fork carrier/continuation contract; marked upstream client validation as upstream-reported and retained scripted Anthropic prohibition.
- release.yml clean merge audited: added PR-history changelog collapsing recognizes both manual (PR #N) and GitHub merge subjects; scans pre-collapse list for breaking changes. Existing fork artifact producer/provenance controls remain unchanged.

## Validation

- All targeted suites run as separate Bun 1.4.2 processes, no live service/provider requests.
- server.test.ts 62/62; codex-usage-refresher.test.ts 17/17; device-setup-lifecycle 6/6; usage-polling-lifecycle 7/7.
- AccountListItem 41/41; RateLimitProgress 32/32; AccountRequestTransformerDialog 4/4.
- OAuth 23/23; request-transformer-update + endpoint threshold exclusion 6/6; account-add-starts-usage-polling 3/3; account-refresh-usage 2/2; accounts-codex-usage-history 8/8; usage-polling-start 5/5; device-setup-http 6/6; oauth-created-account-id 3/3.
- reauth-deadline-alert 4/4; auth-failure-alert 8/8; account-routing-finalization 6/6.
- New endpoint-ineligible HTTP/UI tests were verified failing before implementation, then green. Additional manual-refresh deletion/recreation/endpoint-change and stale threshold-generation tests are green.
- Owned biome check --write: 39 files clean after formatting. Staged git diff --check clean. Parent owns global lint/typecheck/format, candidate-wide tests and PG verification.
- Logs: /tmp/apiui-<test basename>.log; /tmp/ccflare-api-ui-biome.log. Source-only result; no claim that production changed.


# Resolution receipt: providers-resolution

# Provider, adapter, core/types and CLI v3.5.85 resolution

Target 80b593e8cba850b156c9325b98ff680b4aa78010 against fork d61ce67bf; real merge remains owned by parent. All owned conflicts staged, no commits or live provider calls.

## Semantic dispositions

- Codex provider: preserved fork single continuation owner, digest/tail validation, response-id repair, turn-state coordinator, replay and diagnostics. Upstream deleted-method conflicts resolve to the fork architecture, not restored upstream caches. Carried new client stream_options/client_metadata/access_programs through the existing trusted private carrier and added an adapter-to-provider regression proving caller previous_response_id stays ignored. Header hardening now strips cookie/cdn-loop/forwarded/x-real-ip plus existing x-better-ccflare/cf/x-forwarded prefixes; original Headers stay untouched. CODEX_VERSION advances to upstream0.154.0.
- Codex 429 reset semantics: fork already selects latest complete exhausted-window horizon and combines direct Retry-After/reset evidence. Keep stronger fork incomplete-window/no-synthetic-reset contract; upstream first three exhausted-window regressions and non429 regression pass. Adapted upstream reset-only/windowless tests to expect unknown, rather than guessing sooner reset or1h, preserving native quota policy. No duplicate exhaustedWindowResetAt method retained.
- Free usage: canonical public fetchCodexUsageData now comes from upstream usage-endpoint.ts, returns status/planType and accepts options.signal for fork cancellation. Unknown percentages omitted; duration-based slotting/overflow validation/Retry-After fallback preserved. Composed fork symmetric404 remembered endpoint fallback, shared timeout+body deadline, credits/plan/code-review scalar metadata. Legacy api-usage.ts helpers remain available for backwards compatibility; public fetch and poller use hardened endpoint. New usage-endpoint-fork test exercises fallback+metadata+unknown behavior; old API tests remain green.
- Usage cache: retained fork registration identity/abort generations, reactive native-family/model depletion, canonical UsageSnapshotPayload callback and active-window helpers. Added cache write generations to reject Codex polls losing to traffic/manual updates/deletion; retained real-window rollover predicate with injected five-hour policy. Polls never clear real routing cooldown through capacity-restored callbacks. New polling tests adapted to fork snapshot argument contract. Paired representative Zai snapshot now chooses utilization/reset from the same window including time_limit, without changing token-only canonical ledger helpers.
- Responses tools: added upstream flattened namespaces/hash identities, custom freeform declaration bridge and JSON/SSE round trip, rich tool outputs, type-optional messages. Preserved fork tool ID sanitization/lineage/dedup, malformed input handling/warn cap, instruction ordering failures, bounded parsing/SSE frames/output, terminal ownership and sampling. Namespace nesting and combined declaration/content admission bounded; declarations forwarded through Codex native additional_tools carrier while compatible backends receive translated definitions.
- Adapter errors: recognize native/FastAPI detail/message and+json errors, preserve actionable code/extras, retain fork narrowly scoped finite503 recovery headers. Keep upstream x-request-id explicitly; discard content encoding/length by fresh header construction.
- OpenAI response translation: retain exact drain transport and body disposal; upstream omitted-model fallback uses context.requestModel or trusted proxy final-model physical metadata. Proxy account-transform integration tests pass.
- Core/types: add independent usage pause thresholds and manual deadline types without dropping native routing/alerts/reset fields. New5xx and response-alias controls are explicit true opt-ins (default off), preserving fork activation policy. Existing cooldown safety helpers and bounded overload config coexist. Account scalar attempt view preserves six new fields; absent upgrade fields get defaults, present accessors/malformed values rejected without getter invocation; unknown string transformer IDs remain inert in dispatcher.
- CLI: retained lazy initialization, managed-routing commands, generated help/mode registry; added usage threshold parser/handler/help. Threshold control requires supported provider and canonical Codex subscription endpoint. OAuth manual reauth timestamp imported with exact SQL binding regression updated; existing terminal reauth-safe resume preserved.
- Clean merged OpenAI format converters/stream defaults, Codex usage/header parser, new threshold logic and declarations reviewed and exercised.

## Intentional untouched upstream paths

- package.json/apps/cli/package.json kept fork3.5.78 under no-manual-version-bump policy, exact JSON parsed HEAD contents; no version bump.
- core/version.ts remains CLAUDE_CLI_VERSION2.1.250, originated09cf0705 and retained by prior60005baa/v3.5.78 integrations. This is preserved fork client identity, NOT numerically newer than upstream2.1.272. No independent rationale for numeric pin or live-client verification was found; parent should not claim one.
- Codex on-demand-fetch.ts upstream-only change exports isCodexSubscriptionEndpoint. Fork already exports canonical helper from provider.ts via codex/index.ts and providers/index.ts, and on-demand imports that helper; barrel callers receive equivalent behavior with fork endpoint validation.
- CLI README and excluded generated workers untouched.

## Verification

53 isolated per-file Bun runs: 1274 passing tests, 0 failed files. Receipts /tmp/provider-tests-final.json and /tmp/provider-tests/*. All Codex provider test files, adapters, usage-generation/depletion/snapshot, core controls/thresholds, scalar attempt plans, OAuth reauth and OpenAI response processing covered.

Root typecheck passed at final owned state (/tmp/provider-final-typecheck.log). Scoped biome check --write covered370 files, zero errors, three existing noNonNullAssertion warnings in minimax/zai tests (/tmp/provider-biome-final.log). New native fields regression was observed failing before implementation, then passing. Parent owns complete lint/typecheck/format/build/CI verification and reviewer pass.

## Superseding final Claude client-version decision

Independent review found no numeric-pin rationale for retaining2.1.250, and the fork's existing assertion explicitly requires exact upstream Claude fallback parity. The final integration therefore advances only CLAUDE_CLI_VERSION to upstream2.1.272 and updates that exact assertion. This supersedes the earlier core/version.ts intentional-unchanged disposition above. Package release versions stay3.5.78; runtime/build provenance functions and client-observed version precedence are unchanged. Upstream unchanged-path count consequently falls from five to four.

Tests first: updating the assertion alone produced24pass/1fail; changing the literal produced25pass/0fail. Version, usage-fetcher and auto-refresh probe suites total50pass/0fail in four isolated Bun1.4.2 processes. A separate entirely mocked fetch assertion verifies the fallback, claude-code/2.1.272 usage User-Agent and tracked-client precedence without any network dispatch. Receipts /tmp/ccflare-v3.5.85-client-version-results.json and /tmp/ccflare-v3.5.85-client-version-headers.log. Scoped Biome and staged whitespace checks passed. Only version.ts/version.test.ts changed and were staged for this correction.


# Resolution receipt: migration-correction

# Migration review correction

Owned migrations.ts, migrations-pg.ts, migrations-pg.test.ts, new account-rebuild-preservation.test.ts only. Existing integration edits retained; no commits.

Both legacy SQLite accounts rebuilds now preserve request_transformer, last_manual_reauth_at, four threshold settings, billing_type, refresh_token_issued_at, peak_hours_pause_enabled, rate_limited_reason/rate_limited_at/consecutive_rate_limits. Threshold schema/backfill executes before refresh-token rebuild and its projection retains values; per-flag-newness still controls enable backfill, so explicitly disabled windows remain off. Fresh ensureSchema defines all four threshold fields.

SQLite and PostgreSQL dedup choose manual reauthentication timestamp from exactly the same credential donor row as refresh/access/expiry/issued-at, including null. No independent MAX/COALESCE provenance mixing.

Tests-first: initial three rebuild regressions failed (missing manual column and silently reset thresholds), added dedup known/null cases failed, then all five passed. Fresh-schema test failed before adding four columns, then all six passed. Live PG credential test now covers known/null timestamps with credential donor discarded by survivor ranking.

Final 97 tests passed across6 isolated migration suites, including20 live PG tests against the task-owned disposable loopback database. Receipts /tmp/ccflare-rebuild-results.json and /tmp/ccflare-rebuild-*.log. Scoped Biome check clean. Root typecheck receipt /tmp/ccflare-rebuild-typecheck.log.


# Resolution receipt: tool-choice-correction

# Forced tool-choice review correction

Owned adapter handler.ts, request-translator.ts, custom-tools.ts, new tool-choice-admission.test.ts. No commits.

Admission now validates exact effective declaration identity: function/custom kind, nonempty string name, optional nonempty string namespace. Namespace matching occurs after flattening/deduplication and before hashing. Unknown/malformed/undeclared choices return the existing400 invalid_request_error envelope with zero provider dispatch. Declaration namespaces are also validated. Valid custom/function namespaced, top-level and Responses Lite additional_tools choices retain existing conversion. Shared isNamedToolChoice shape guard prevents direct translator crashes from malformed runtime values.

Tests-first regression observed18 failures before implementation;25 new admission/direct translation tests now pass. Full adapter suite passes 252 tests across9 isolated files. Receipts /tmp/ccflare-tool-choice-before.log, /tmp/ccflare-tool-choice-after.log, /tmp/ccflare-tool-choice-results.json, /tmp/ccflare-choice-*.log. Scoped Biome clean. Typecheck receipt /tmp/ccflare-tool-choice-typecheck.log. Independent reviewer notified for original repro rerun.


# Resolution receipt: native-tool-correction

# Native Responses tool identity correction

Completed in shared upstream-v3.5.85 integration worktree; staged six owned files only, no commit, push or live provider request.

## Findings fixed

- Original custom/namespaced declarations were restored on the Codex wire while forced choices retained the compatibility alias and function kind.
- Additional-tools-only native input also exposed synthetic bridge declarations in top-level tools.
- Replayed native custom/namespaced calls lost name/namespace/kind and custom output kind through the Anthropic intermediate form.

## Final behavior

- Adapter carries the already-admitted original tool choice and identities for only the calls/results actually emitted by ordinary translation, after duplicate suppression and call ID sanitization.
- Codex restores native tool declarations, choices, and replay identities only on the existing proxy-verified native Responses path. Plain Messages body injection cannot supply this native tool control metadata.
- Native additional_tools excludes the bridge-only top-level tools array. Compatible backends retain the same translated tool declarations and forced choice.
- Replay restoration happens after ordinary conversion and before continuation owner/digest evaluation; no raw input replacement, caller previous_response_id reuse, or tail-check bypass. Arguments/results retain normal conversion and custom input only unwraps the schema bridge.
- Current Agent/Task orchestration filtering applies to original native declarations and choices as well. Direct namespaced function declarations correctly mark the request as requiring native response preservation.

## Tests

- New native-tool-identity.test.ts: before implementation 2 pass/13 fail; after implementation 15 pass/0 fail. Matrix covers bare/namespaced function/custom, regular/additional declarations, forced choices, native replay and paired output types with sanitized IDs, incoming private-carrier replacement, plain Messages native metadata rejection, and attributed Agent/Task filtering.
- 42 isolated Codex+Responses-adapter suites: 1,134 pass, 0 fail, all exit 0. Receipt /tmp/ccflare-native-tool-results.json; logs /tmp/ccflare-native-tool-suite-*.log.
- Root typecheck passes (/tmp/ccflare-native-tool-typecheck.log).
- Scoped Biome check/format passes without warnings and staged whitespace check clean. Parent owns full lint/format/build/final repository suite.
- Existing direct additional_tools provider test now explicitly supplies the proxy-verified native marker, as required by the production path.

Independent reviewer has been notified to rerun original offline repros with that same verified marker (the direct callback repro otherwise skips proxy verification).

## Final source addendum: Claude fallback parity

The later final integration review supersedes the earlier provider report's retained2.1.250 exception: CLAUDE_CLI_VERSION now uses exact upstream2.1.272, with the existing parity assertion updated. Package versions/provenance functions remain unchanged. This two-file literal/assertion correction does not change the native tool composition described above. Four isolated focused suites passed50tests and an offline mocked usage-header check passed; receipt /tmp/ccflare-v3.5.85-client-version-results.json. No provider traffic was sent.


# Resolution receipt: caller-audit

# Changed caller and mock contract audit

Read-only audit in `/home/will/SITES/better-ccflare/.claude/worktrees/upstream-v3.5.85`, 2026-09-15. Searches used `rg -a -n` and explicit exclusions for all four prohibited generated inline workers. No repository edits or provider traffic. This supplements the parent's isolated suite and gated PostgreSQL runs; it does not claim test files were typechecked.

## Searched contracts and scope

- `markAccountRateLimited`:195 text matches across packages/apps, including facade, repository, production consumers and mocks.
- `pauseForUsageThreshold|resumeFromUsageThreshold|pauseAccountForUsageThreshold|resumeAccountFromUsageThreshold`:25 matches.
- `UsageSnapshotPayload|startPolling|usage-polling-start`:89 matches.
- `last_manual_reauth_at|request_transformer|usage_pause_five_hour_threshold|usage_pause_weekly_threshold`:247 matches.
- Also audited `mock.module` providers/http-api/proxy barrel stubs; startUsagePollingForNewAccount, polling registration exports, and delayed cooldown promises. Raw search receipts are `/tmp/ccflare-v385-callers-{0,1,2,3}.txt`.

## Findings requiring follow-up

### Stale resolved-number rate-limit mocks

The production method now returns `{ consecutiveRateLimits, applied }`, but these test definitions still resolve a number. Current broad tests can stay green because they do not assert the persisted counter: the awaited consumer reads `result.consecutiveRateLimits`, producing undefined for these mocks.

- `packages/proxy/src/handlers/__tests__/response-processor.test.ts:832` (return1 at841).
- `packages/proxy/src/handlers/__tests__/proxy-operations-codex-websocket.test.ts:104,2644,2753`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-codex-reasoning.test.ts:81`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-402-routing.test.ts:194`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-model-observability.test.ts:97`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-codex-response-id-repair.test.ts:119`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-clear-thinking.test.ts:110`.
- `packages/proxy/src/__tests__/proxy-combo-fallback.test.ts:1099,1268`.
- `packages/proxy/src/handlers/__tests__/response-body-cancel-on-failover.test.ts:81`.
- `packages/proxy/src/handlers/__tests__/proxy-operations-out-of-credits.test.ts:1271,1321`: deferred<number> passed to the mock and later resolved with1.

Additional no-op mocks return undefined in response-processor-clone-cancel (124,323), response-processor-parseusage-disposal (113), server-tool-routing.integration (208). Those are success/no-mutation fixtures, so no exercised failure was established, but typed result mocks would prevent future hidden drift. The hanging Promise<number> at pool-exhausted319 never resolves and does not exercise result shape.

### Applied-result visibility in awaited persistence

`database-operations.ts:1057` forwards the repository result object correctly. The sync cooldown consumer (`rate-limit-cooldown.ts:437`) consumes both fields and logs an actual skipped write. The awaited writer adapter (`rate-limit-cooldown.ts:531-538`) projects only consecutiveRateLimits; it cannot distinguish a concurrent DB forward-guard rejection from an applied write in its final log. The new5xx path's circuit receipt is truthful for its in-memory forward guard, but does not distinguish the stronger-bench race discovered only by the DB write. This is an observability/circuit-receipt edge case to assess, not a broken return-object caller. No runtime reproduction was performed in this read-only audit.

## Compatible caller contracts confirmed

- Threshold facade and repository methods accept optional third expectedCreatedAt; facade forwards it and both SQL writes append created_at predicate and parameter together. The server's production calls at server.ts388/397 forward expectedCreatedAt. Existing two-argument callers remain source-compatible. SQLite stale-incarnation coverage in account-usage-threshold-pause.test.ts157-172 covers rejected stale and accepted current writes. Server generation fence coverage exists at server.test.ts1577. No PG-specific threshold callers use an incompatible shape; SQL uses portable COALESCE and adapter placeholders.
- UsageSnapshotPayload is exported from usage-fetcher and providers index's export-star. startPolling keeps optional snapshot callback at argument8, stale-weekly callback at9; server registrar matches argument8 and production callsites preserve positions. Existing short-arity test registrar functions can ignore added optional arguments. Provider usage-cache-generation and codex-usage-polling tests cover registration lifecycle and snapshot delivery.
- New polling helper is internal HTTP service `services/usage-polling-start.ts`, with handler compatibility re-export. OAuth, accounts and device-setup production imports resolve directly to service. Its dependency restartUsagePollingForAccount is present in proxy index. Tests import the compatibility module and use actual registered restarters. No missing http-api root export is required by a current caller.
- Audited mock barrels: device-setup-lifecycle carries newly imported Codex helper symbols; device-setup-http and oauth-created-account-id carry restartUsagePollingForAccount; account-force-reset-rate-limit and account-remove-cleanup-contract already carry it. OAuth/complete-reauth use real-module spreading/restoration for added exports. No missing exercised barrel symbol identified.
- New Account fields survive immutable provider attempt snapshots: request transformer, manual reauth timestamp, both threshold percentages and enabled flags. Absent legacy fixture fields default to null/false; own-property readers continue rejecting getters/invalid scalar types. provider-attempt-plan.test.ts covers transformer snapshot retention and invalid property handling. Server-owned account routing defaults also include the new fields; repository SELECTs and Account mapping include them.

## Gated PostgreSQL evidence boundary

Parent reports actual candidate PG migration19/API59 plus later migration20 runs passed; audit did not rerun or replace those. `pg-live-queries.test.ts:1479` awaits the new result object without a numeric assumption, so it is call-compatible. Its test title promises a truthful applied flag but only asserts toBeDefined plus persisted row state: it does not itself pin object fields or the rejected transient-write case. Generic repository forward-guard tests do assert `{ consecutiveRateLimits, applied }` against SQLite. This is a PG assertion-strength gap, not evidence of a PG runtime bug.


## Caller-audit corrective follow-up completed

Full MarkAccountRateLimitedResult now flows through active and pending single-flight writes. Awaited helper returns the DB receipt, false for a suppressed in-memory write, or null for the existing bounded failure/timeout fallback. A concurrent durable stronger bench produces a truthful skipped log and5xx attempt/circuit receipt; candidate expiry is not claimed when rejected. The regression started red and passes; coalesced false receipts are asserted. All12direct numeric and2deferred numeric test definitions were corrected, plus4undefined no-op definitions. PostgreSQL test now asserts successful result shape and rejected transient-write result, unchanged longer durable hold/reason and unchanged quota streak.

Verification:16 affected isolated suites396tests pass, including live PostgreSQL59tests/258assertions; typecheck green. Owned18files formatted/staged. Receipt /tmp/ccflare-v385-receipt-verified.json. No migration code edited. The earlier report's numeric-mock, dropped-applied and PG assertion-gap findings are resolved.


## Coalesced write ownership follow-up completed

Single-flight sharing now preserves mutation ownership as well as DB outcome. A caller joining an active stronger/equal write receives its authoritative consecutive count with applied=false. Pending writes carry an owner token; replacing the pending payload with a strictly stronger deadline transfers ownership to that caller. Superseded, weaker and tied followers await the write but cannot claim it; the final payload owner receives the real DB receipt. Bounded timeout/failure behavior is unchanged.

Tests first: active mixed429/shorter5xx and pending intermediate/max/tie receipt expectations failed before fix. After fix helper8/8 and5xx26/26 pass; bounded lint and typecheck green. Only rate-limit-cooldown.ts and its persist-bound test changed, both staged. Parent runs final full verification; independent reviewer has stable source notice.


# Proxy and database resolution receipt

# Proxy and database repository integration notes

Scope: merge incoming 80b593e8cba850b156c9325b98ff680b4aa78010 into fork d61ce67bf. Owned proxy code/tests plus database facade/repository/test reconciliation. All owned changes staged, no commit/push. Original merge reported recorded preimages only; no reused rerere resolutions. No live provider traffic.

## Semantic integration

- Transient 500/502/503/504 retries and short benches compose with the fork physical-attempt ledger, degraded reservations, raw-response classification, transport ownership and hosted-dispatch no-replay veto. Shared overload/server-error retry machinery preserves upstream sequential budgets while the fork global attempt cap remains authoritative. Analytics records a routing child attempt instead of an extra inbound request. Last candidate forwards the real upstream status/body. Both failover and terminal paths await the fork bounded durable cooldown writer before leaving the account; analytics queue backlog cannot defer routing control. Server retry is explicitly opt-in: CCFLARE_SERVER_ERROR_RETRY_ENABLED=true.
- Rebuilt transport requests use adoptRetryTemplate, consuming each body once into scalar URL/method/headers/body text; no unread native Request clone remains as the replay template. This fixes upstream's replay-state cases within the fork attempt-plan architecture: thinking removal, cache-control removal, model fallback and further in-place retries all replay the latest body. Incoming drain tests also fixed the replacement-response drain before another replay.
- Account request transformer runs after provider transform through the immutable attempt plan, including retry transforms, and retains the upstream openai-compatible provider gate. Buffered request consumption replaces upstream clone().text().
- Provider-parsed 429 reset outranks stale usage cache reset, with the upstream eight-day parser bound. Fork confirmed-reset semantics and configurable twelve-hour default safety ceiling remain; only unattributed scope uses the short ramp. Response errors do not clear an existing quota hold. Transient 5xx benches never increment quota streak or shorten a stronger bench. They record one circuit failure after the bounded durable write when the cooldown applies, preserving the truthful circuitCounted receipt; a stronger active bench suppresses the write and circuit count.
- Incoming response-model aliasing is available only with CCFLARE_RESPONSE_MODEL_ALIAS_ENABLED=true. Default physical-model wire identity remains intact. Accounting sees original upstream bytes and physical served-model provenance before downstream alias rewriting. SSE wrappers have highWaterMark=0 so wrapping alone cannot consume a terminal frame and advance route success.
- Auto-refresh incorporates exhaustion skip/announcements, deadline warning computation and local refusal detection. Fork generation/deletion fences and native account eligibility remain. Only a verified internal probe can target the exact temporarily unavailable account; terminal reauth stays fail-closed. Local refusal exceptions require the known local 503 shape and the account still being benched. Both fork service_unavailable/route_unavailable and force_route_unavailable wire shapes are recognized.
- Native continuation carrier and custom-tools behavior stay on the fork trust/ownership implementation; the upstream parallel cache/native-header mechanism is unnecessary. Raw retry classification is provided by the fork existing classifier with incoming billing-400 passthrough preserved. Request-model response context is transmitted by the existing trusted final-model header, consumed by providers worker.
- Codex rollover uses upstream reset-boundary plus utilization-drop logic while retaining fork canonical window/ledger integration. The existing rollover fixture was corrected to demonstrate a real utilization decrease.
- DB threshold pause/resume facade/repository methods accept optional expectedCreatedAt; SQL guards created_at when supplied so late async usage polls cannot pause/resume a replaced account. Incoming schema and fields preserved. Shared rate-limit persistence returns consecutiveRateLimits plus applied result, retaining fork awaited persistence and write-order protections. Migration rebuild fixes are owned by integrate_providers separately.

## Explicit fork behavior retained

- Forced-account requests remain fail-closed, including missing/failed-lookup and ordinary paused accounts, instead of falling back to arbitrary accounts. Upstream selector tests assert the fork typed refusal.
- Empty/unresolved pool is route_unavailable, while known exhausted pools retain pool_exhausted. The existing cache-telemetry enum only classifies known pool exhaustion, so the new joined local-refusal test expects null telemetry refusal_reason for unknown route, with gateway join digests and wire route_unavailable separately verified.
- Neither new 5xx retries nor response model aliasing activates by default. Existing fork physical-account routing tests are unchanged and pass under the default configuration.

## Verification

Pinned Bun 1.4.2. All 29 changed owned test files passed in isolated invocations: 632 tests, zero failures; the subsequent independent-review fixes added two blocked-writer/DB tests plus a stronger-bench/circuit suppression test, with the expanded 5xx suite25/25 green (635 total owned tests). Receipt /tmp/ccflare-v385-owned-verified.json with per-file logs. Additional original fork semantic-failover and combo-fallback suites passed, plus native-quota-physical-fallback in earlier rerun. Focused final receipt /tmp/ccflare-v385-owned-final.json. Owned48 files formatted via Biome. Parent owns final global lint/typecheck/build/full-isolated run and PG migration rehearsal.

Meaningful new delta tests: alias wrapper does not prefetch/observe terminal completion before a downstream read; 5xx retries stop at the fork physical budget and after a hosted-dispatch claim; retries remain off for unset/false config; provider reset honors widened ceiling and preserves default twelve-hour cap; 5xx bench persistence bypasses a blocked analytics queue and is awaited on both terminal/failover paths; stale account incarnation cannot pause or resume replacement; fork-shaped local refusals do not count against the already-benched auto-refresh account.

Remaining owned blockers: none at this handoff. Independent proxy review and parent's broader checks may discover additional findings.


## Caller-audit corrective follow-up completed

Full MarkAccountRateLimitedResult now flows through active and pending single-flight writes. Awaited helper returns the DB receipt, false for a suppressed in-memory write, or null for the existing bounded failure/timeout fallback. A concurrent durable stronger bench produces a truthful skipped log and5xx attempt/circuit receipt; candidate expiry is not claimed when rejected. The regression started red and passes; coalesced false receipts are asserted. All12direct numeric and2deferred numeric test definitions were corrected, plus4undefined no-op definitions. PostgreSQL test now asserts successful result shape and rejected transient-write result, unchanged longer durable hold/reason and unchanged quota streak.

Verification:16 affected isolated suites396tests pass, including live PostgreSQL59tests/258assertions; typecheck green. Owned18files formatted/staged. Receipt /tmp/ccflare-v385-receipt-verified.json. No migration code edited. The earlier report's numeric-mock, dropped-applied and PG assertion-gap findings are resolved.


## Coalesced write ownership follow-up completed

Single-flight sharing now preserves mutation ownership as well as DB outcome. A caller joining an active stronger/equal write receives its authoritative consecutive count with applied=false. Pending writes carry an owner token; replacing the pending payload with a strictly stronger deadline transfers ownership to that caller. Superseded, weaker and tied followers await the write but cannot claim it; the final payload owner receives the real DB receipt. Bounded timeout/failure behavior is unchanged.

Tests first: active mixed429/shorter5xx and pending intermediate/max/tie receipt expectations failed before fix. After fix helper8/8 and5xx26/26 pass; bounded lint and typecheck green. Only rate-limit-cooldown.ts and its persist-bound test changed, both staged. Parent runs final full verification; independent reviewer has stable source notice.
